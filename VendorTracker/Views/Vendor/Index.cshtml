@using VendorTracker.Models;
@using System.Collections.Generic;

<h2>Your added Vendors</h2>
@if (Model.Count == 0)
{
  <p>There are no vendors associated with your store yet!</p>
}
<ol>
  @foreach (Vendor element in Model)
  {
    <li><a href="/vendors/@element.Id">@element.Name</a></li>
  }
</ol>