@using System;
@using System.Collections.Generic;
@using VendorTracker.Models;

<h1>@Model["vendor"].Name</h1>

<ul>
  @foreach (Order element in Model["orders"])
  {
    <li>@element.Type : @element.Quantity</li>
  }
</ul>